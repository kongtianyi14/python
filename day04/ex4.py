"""
    容器----字符串
    由一系列字符组成的不可变序列容器，存储的是字符的编码值。

    编码
    ord 可以见字符变成对应ASCII码的值
    chr 反之，将数字转换成对应表中的字符
"""
number = ord("a")
print(number)
# 打印出来的值对应的就是字符a在表中的值

character = chr(97)
print(character)
